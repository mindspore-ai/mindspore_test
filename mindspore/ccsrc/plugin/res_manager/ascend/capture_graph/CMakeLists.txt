if(ENABLE_D OR ENABLE_ACL)
  file(GLOB _ASCEND_CAPTURE_GRAPH_SRC_LIST RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} "*.cc")
  set_property(SOURCE ${_ASCEND_CAPTURE_GRAPH_SRC_LIST} PROPERTY COMPILE_DEFINITIONS
    SUBMODULE_ID=mindspore::SubModuleId::SM_DEVICE)
  add_library(_mindspore_ascend_capture_graph_obj OBJECT ${_ASCEND_CAPTURE_GRAPH_SRC_LIST})
endif()