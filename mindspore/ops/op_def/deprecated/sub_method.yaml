#deprecated sub
sub:
  args:
    input:
      dtype: tensor
      type_cast: number, list[any], tuple[any]
    y:
      dtype: tensor
      type_cast: number, list[any], tuple[any]
  returns:
    outputs:
      dtype: tensor
