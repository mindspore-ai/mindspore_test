#EmbeddingFeatureMappingV2
embedding_feature_mapping_v2:
    args:
        table_name:
            dtype: str
        feature_id:
            dtype: tensor
        table_total_size:
            dtype: tuple[int]
            type_cast: list[int]
        table_actual_size:
            dtype: tuple[int]
            type_cast: list[int]
    returns:
        offset_id:
            dtype: tensor
