#EmbeddingFeatureMappingInsert
embedding_feature_mapping_insert:
    args:
        table_name:
            dtype: tensor
        num:
            dtype: int
        feature_id:
            dtype: tuple[tensor]
            type_cast: list[tensor]
        offset_id:
            dtype: tuple[tensor]
            type_cast: list[tensor]
    returns:
        fake_output:
            dtype: tensor
