#operator broadcast_to
broadcast_to:
  args:
    input:
      dtype: tensor
    shape:
      dtype: tuple[int]
      prim_init: True
      type_cast: list[int], tensor
  returns:
    output:
      dtype: tensor
  view: True
  graph_view: True
  labels:
    side_effect_mem: True
  dispatch:
    enable: True