#operator reshape
reshape_view:
  args:
    input:
      dtype: tensor
    shape:
      dtype: tuple[int]
      type_cast: tensor, list[int]
  returns:
    output:
      dtype: tensor
  graph_view: True
  dispatch:
    enable: True
    CPU: None
    GPU: None
