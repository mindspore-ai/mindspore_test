#operator select_ext_view
select_ext_view:
    args:
        input:
            dtype: tensor
        dim:
            dtype: int
        index:
            dtype: int
    returns:
        output:
            dtype: tensor
    view: True
    graph_view: True
    labels:
        side_effect_mem: True
    class:
        name: SelectExtView
    dispatch:
        enable: True
