#EmbeddingFeatureMappingFileSize
embedding_feature_mapping_file_size:
    args:
        file_path:
            dtype: str
        table_name:
            dtype: tensor
        embedding_dim:
            dtype: tuple[int]
            type_cast: list[int]
        only_offset_flag:
            dtype: bool
            default: True
    returns:
        feature_size:
            dtype: tensor
