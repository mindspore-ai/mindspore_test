#EmbeddingFeatureMappingFind
embedding_feature_mapping_find:
    args:
        table_name:
            dtype: tensor
        feature_size:
            dtype: tensor
        num:
            dtype: int
            default: 1
    returns:
        outputs:
            dtype: tuple[tensor]
