#EmbeddingFeatureMappingExport
embedding_feature_mapping_export:
    args:
        file_path:
            dtype: str
        teble_name:
            dtype: tensor
        values:
            dtype: tensor
        embedding_dim:
            dtype: tuple[int]
            type_cast: list[int]
        feature_id:
            dtype: tuple[tensor]
            type_cast: list[tensor]
        offset_id:
            dtype: tuple[tensor]
            type_cast: list[tensor]
    returns:
        fake_output:
            dtype: tensor
