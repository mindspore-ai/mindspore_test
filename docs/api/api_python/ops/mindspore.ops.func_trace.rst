mindspore.ops.trace
===================

.. py:function:: mindspore.ops.trace(input)

    返回tensor对角线的元素总和。

    .. note::
        输入必须是Tensor，复数类型暂不支持。

    参数：
        - **input** (Tensor) - 输入tensor。

    返回：
        Tensor