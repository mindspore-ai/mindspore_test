mindspore.ops.unstack
=======================

.. py:function:: mindspore.ops.unstack(input_x, axis=0)

    按指定轴对输入tensor进行拆分。

    参数：
        - **input_x** (Tensor) - 输入tensor。
        - **axis** (int) - 指定轴。默认 ``0`` 。

    返回：
        多个tensor组成的tuple。