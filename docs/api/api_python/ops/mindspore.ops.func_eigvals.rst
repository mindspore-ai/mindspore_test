mindspore.ops.eigvals
=====================

.. py:function:: mindspore.ops.eigvals(A)

    计算输入方阵的特征值。

    .. warning::
        这是一个实验性API，后续可能修改或删除。

    参数：
        - **A** (Tensor) - shape为 :math:`(*, N, N)` 的输入tensor。

    返回：
        Tensor
