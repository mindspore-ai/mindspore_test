mindspore.ops.shuffle
=====================

.. py:function:: mindspore.ops.shuffle(x, seed=None)

    沿着第一维随机打乱tensor。

    参数：
        - **x** (Tensor) - 输入tensor。
        - **seed** (int, 可选) - 随机种子，默认 ``None`` ，等同于 ``0`` 。

    返回：
        Tensor
