mindspore.ops.flipud
=====================

.. py:function:: mindspore.ops.flipud(input)

    将输入Tensor中每一列的元素沿上下方向进行翻转，同时保持矩阵的行数不变。

    参数：
        - **input** (Tensor) - 输入Tensor。

    返回：
        Tensor，翻转后的结果。

    异常：
        - **TypeError** - `input` 不是Tensor。
