mindspore.ops.permute
=====================

.. py:function:: mindspore.ops.permute(input, axis)

    按照指定轴重排输入tensor。

    参数：
        - **input** (Tensor) - 输入tensor。
        - **axis** (tuple(int)) - 指定顺序的轴。

    返回：
        Tensor
