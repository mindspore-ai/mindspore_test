mindspore.ops.angle
===================

.. py:function:: mindspore.ops.angle(input)

    逐元素计算输入tensor的辐角。

    .. math::
        output_i = angle(input_i)

    参数：
        - **input** (Tensor) - 输入tensor。

    返回：
        Tensor

