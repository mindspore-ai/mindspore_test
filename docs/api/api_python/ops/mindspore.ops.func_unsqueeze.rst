mindspore.ops.unsqueeze
=========================

.. py:function:: mindspore.ops.unsqueeze(input, dim)

    在输入tensor的指定维度上增加一维。

    参数：
        - **input** (Tensor) - 输入tensor。
        - **dim** (int) - 指定维度。

    返回：
        Tensor