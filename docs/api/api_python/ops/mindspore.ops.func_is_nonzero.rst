mindspore.ops.is_nonzero
========================

.. py:function:: mindspore.ops.is_nonzero(input)

    判断输入tensor是否包含 ``0`` 或 ``False`` 。输入只能是单元素。

    参数：
        - **input** (Tensor) - 输入tensor。

    返回：
        Bool
