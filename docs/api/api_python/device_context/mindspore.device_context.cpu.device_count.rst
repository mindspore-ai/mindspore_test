mindspore.device_context.cpu.device_count
==========================================

.. py:function:: mindspore.device_context.cpu.device_count()

    返回可用的CPU数量。

    .. note::
        - 对于 ``"CPU"`` 硬件，固定返回1。

    返回：
        int，可用的CPU数量。
