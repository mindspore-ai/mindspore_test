mindspore.mint.concat
=====================

.. py:function:: mindspore.mint.concat(tensors, dim=0)

    :func:`mindspore.mint.cat` 的别名。

    .. warning::
        - 这是一个实验性API，后续可能修改或删除。
        - 图模式下的动态shape场景不支持输入Tensor类型不一致。
