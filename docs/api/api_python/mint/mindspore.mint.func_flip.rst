mindspore.mint.flip
===================

.. py:function:: mindspore.mint.flip(input, dims)

    沿指定维度翻转tensor中元素。

    参数：
        - **input** (Tensor) - 输入tensor。
        - **dims** (Union[list[int], tuple[int]]) - 翻转的维度。

    返回：
        Tensor
