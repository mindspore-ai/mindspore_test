mindspore.mint.ravel
====================

.. py:function:: mindspore.mint.ravel(input)

    沿着0轴方向，将多维Tensor展开成一维。

    参数：
        - **input** (Tensor) - 要展开的Tensor。

    返回：
        一维Tensor，含有与输入相同的元素。

    异常：
        - **TypeError** - `input` 不是Tensor。
