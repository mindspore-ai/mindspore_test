mindspore.numpy.fliplr
=================================

.. py:function:: mindspore.numpy.fliplr(m)

    在左右方向上翻转每行中的元素。单个列保持不变，但列之间的顺序会发生改变。

    参数：
        - **m** (Tensor) - 输入数组。

    返回：
        Tensor。

    异常：
        - **TypeError** - 如果输入不是Tensor。