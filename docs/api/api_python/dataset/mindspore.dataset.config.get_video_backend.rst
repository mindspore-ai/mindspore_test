mindspore.dataset.config.get_video_backend
==========================================

.. py:function:: mindspore.dataset.config.get_video_backend()

    返回当前用于解码视频的后端。

    返回：
        str，用于解码视频的后端。
